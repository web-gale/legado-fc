{
  "name": "legado-fc-github-pages",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/prepare-build.mjs && vite build && node scripts/finalize-build.mjs",
    "preview": "vite preview",
    "seed": "node scripts/export-seed.mjs",
    "test": "npm run build && node --import tsx --test tests/game-engine.test.ts && node --test tests/static-build.test.mjs"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "tsx": "latest",
    "typescript": "latest"
  }
}
